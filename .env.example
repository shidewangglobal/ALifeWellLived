GEMINI_API_KEY=your_key_here
# Optional: set a specific model if you want (otherwise the server auto-picks).
# Examples might include: gemini-2.0-flash, gemini-2.0-pro, etc.
GEMINI_MODEL=

# ID of the Google Doc that contains Joy's rules.
# Example link: https://docs.google.com/document/d/THIS_IS_THE_ID/edit
JOY_RULE_DOC_ID=your_google_doc_id_here

# Optional: extra Google Docs from Drive that Joy can read (comma-separated IDs).
# Each doc must be shared "Anyone with the link can view".
# Example: JOY_KNOWLEDGE_DOC_IDS=id1,id2,id3
JOY_KNOWLEDGE_DOC_IDS=

# Optional: read ALL Google Docs inside this folder (and subfolders). Overrides JOY_KNOWLEDGE_DOC_IDS when set.
# Get folder ID from Drive URL: https://drive.google.com/drive/folders/FOLDER_ID
JOY_DRIVE_FOLDER_ID=

# Path to service account JSON key (for Drive API). Required when using JOY_DRIVE_FOLDER_ID.
# Example: ./credentials/drive-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=
# Alternative for deploy (Render/Railway): paste the full JSON key as one line (no file upload).
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}

# WhatsApp Cloud API (Meta) settings
# Used to verify your webhook (you choose any secret string)
WHATSAPP_VERIFY_TOKEN=your_verify_token_here
# Graph API permanent access token (WhatsApp)
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token_here
# Phone number ID from WhatsApp Cloud API setup
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here

