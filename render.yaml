services:
  - type: web
    name: daily-fortune
    runtime: node
    plan: free
    autoDeploy: true
    rootDir: daily-fortune
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
